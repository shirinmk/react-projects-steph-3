// 1 created react app, index.html, index.js, App.js and created component folder in src
//  I don't know how the api show the image and get that url??


// App is big parent which has child api who get term and connect to your api account on server and get 10 images
// then this api child gives all images to parent App
// Then parent App gives array of images to another child whoes name is imageList
// imageList child then distribute all images to its children ImageShow 
//imageList use state of array and function has map and pass this function to its body jsx Then pass state varible to its child ImageShow
